# Upstash Kafka Configuration
# Replace xxx-xxxxx-xxxxx with your actual Upstash cluster details

# Kafka Brokers (from Upstash Dashboard → Details → Bootstrap Servers)
KAFKA_BROKERS=xxx-xxxxx-xxxxx.upstash.io:9092

# SASL Authentication (from Upstash Dashboard → Details)
KAFKA_SASL_USERNAME=your-upstash-username
KAFKA_SASL_PASSWORD=your-upstash-password
KAFKA_SASL_MECHANISM=scram-sha-256

# SSL/TLS (Upstash requires SSL)
KAFKA_SSL=true

# Kafka Client Configuration
KAFKA_CLIENT_ID=kafka-bridge-upstash
KAFKA_GROUP_ID=kafka-bridge-group-1

# Kafka Topics (must match topics created in Upstash)
TOPIC_UPSERTS=employee-upserts
TOPIC_DELTAS=employee-deltas

# Normalizer HTTP Configuration
NORMALIZER_BASE_URL=https://normalizer-298832040055.us-central1.run.app
INGESTION_TOKEN=af678b9c6f6602f2027bae756a2aa6b6e0a8ac664a67cfbb

# Batch & Retry Tuning (Green Defaults)
BATCH_MAX_ROWS=1000
BATCH_MAX_MS=1200
HTTP_TIMEOUT_MS=15000
RETRY_BASE_MS=500
RETRY_MAX_MS=15000
MAX_RETRIES=8
CONCURRENCY=1

# Health Server
PORT=8080
